-- Auto-generated by `aria export --gate-model` — DO NOT EDIT.
-- Gate-model state-preparation obligation: the emitted circuit is
-- definitionally the library circuit, and the toolkit's *canonical*
-- state-prep (+ unitary) obligation is CLOSED sorry-free by a proved
-- theorem in proofs/lean4/QuantumProofs (no `sorry`). The `@prove` name
-- is the theorem label only, not a translation of the free-form body.

import QuantumProofs.CircuitSemantics
import QuantumProofs.Gates
import QuantumProofs.BellPrep

namespace Exported.GateModel.Bell

open QuantumProofs QuantumProofs.CircuitSemantics

/-- Gate-model circuit for `Bell` (2 qubits). -/
def circuit : Circuit 2 :=
  [.h ⟨0, by omega⟩,
   .cx ⟨0, by omega⟩ ⟨1, by omega⟩ (by decide)]

/-- The emitted circuit is definitionally the library circuit. -/
theorem circuit_eq : circuit = QuantumProofs.BellPrep.bell_circuit := rfl

/-- **State-preparation correctness** — column 0 of `denote circuit`
    (its action on |0…0⟩) matches `QuantumProofs.BellPrep.bellAmp`. Closed sorry-free. (Name from the
    `@prove` label; statement is the toolkit's canonical state-prep.) -/
theorem bell_correct :
    ∀ i : Fin (2^2), denote circuit i 0 = QuantumProofs.BellPrep.bellAmp i := by
  rw [circuit_eq]; exact QuantumProofs.BellPrep.bell_state_prep_correct

/-- **@assert unitary** — `denote circuit` is unitary. Closed sorry-free. -/
theorem circuit_unitary : is_unitary (denote circuit) := by
  rw [circuit_eq]; exact QuantumProofs.BellPrep.bell_unitary

end Exported.GateModel.Bell
