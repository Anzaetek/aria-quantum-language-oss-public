-- Auto-generated by `aria export --gate-model` — DO NOT EDIT.
-- Gate-model QPE FAITHFULNESS obligation (LEAN_EXPORT Track B): the actual
-- (n+1)-qubit QPE circuit — X eigenstate prep, H^⊗n, controlled-phase
-- cascade, and the explicit inverse-QFT GATE LIST — measured on the
-- counting register yields the phase m with probability 1 for exact
-- θ = m/2^n. CLOSED sorry-free by `QuantumProofs.QPEFaithful.qpe_faithful`
-- (no `sorry`, no `native_decide`, no opaque matrix-adjoint caveat).
-- NOTE: the library model `qpeCircuit n` carries the eigenstate on qubit 0;
-- `qpe_recognized` verified the emitted circuit matches it gate-for-gate.

import QuantumProofs.QPEFaithful

namespace Exported.GateModel.QPE

open Matrix QuantumProofs.QPEFaithful QuantumProofs.CircuitSemantics

/-- Gate-model QPE circuit for `QPE` (3+1 qubits: eigenstate on qubit 0,
    3 counting qubits): the library model `qpeCircuit 3 θ` (faithful to the
    X-prep + H-layer + controlled-phase cascade + inverse-QFT gate list). -/
noncomputable def circuit (θ : ℝ) : Circuit (3 + 1) := qpeCircuit 3 θ

/-- **QPE faithfulness** — for exact phase `θ = m/2^3`, measuring the counting
    register of `denote circuit` yields `m` with probability 1. Closed sorry-free.
    (Statement is the toolkit's canonical QPE phase-recovery obligation.) -/
theorem qpe_recovers_phase (m : Fin (2 ^ 3)) (θ : ℝ) (hθ : θ = ↑m.val / ↑(2 ^ 3)) :
    counting_marginal (denote (circuit θ) *ᵥ basis0 (3 + 1)) m = 1 :=
  qpe_faithful 3 m θ hθ

end Exported.GateModel.QPE
