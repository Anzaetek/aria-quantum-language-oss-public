-- Auto-generated by `aria export --gate-model` — DO NOT EDIT.
-- Gate-model GROVER measurement obligation (LEAN_EXPORT Track B): the actual
-- 3-qubit Grover circuit — uniform H^⊗3 layer then `optimal_iterations 3`
-- repetitions of (oracle · diffusion) for the canonical marked item |111⟩ —
-- run on the uniform state and measured yields the marked item with
-- probability ≥ 1 − 1/8. CLOSED sorry-free by
-- `QuantumProofs.GroverCircuit.grover_gate_optimal_success` (no `sorry`, no
-- `native_decide`).
-- NOTE: the library model `groverIterTopPow k` uses the `ccz` primitive for
-- the multi-controlled Z; the toolkit lowering (grover3.aria) emits the
-- standard `H · CCX · H` MCZ decomposition. `grover_recognized` verified the
-- emitted circuit matches the canonical gate-model Grover(3) for |111⟩.

import QuantumProofs.GroverCircuit

namespace Exported.GateModel.Grover

open Matrix QuantumProofs.GroverCircuit QuantumProofs.Grover QuantumProofs.CircuitSemantics

/-- Gate-model Grover circuit for `Grover` (3 qubits): the library model
    `groverIterTopPow (optimal_iterations 3)` — the uniform H-layer followed by
    `optimal_iterations 3` Grover iterations (oracle · diffusion) for the
    canonical marked item |111⟩. -/
noncomputable def circuit : Circuit 3 := groverIterTopPow (optimal_iterations 3)

/-- **Grover measurement correctness** — running `denote circuit` on the uniform
    state and measuring yields the marked item |111⟩ (index 7) with probability
    ≥ 1 − 1/8. Closed sorry-free. (Statement is the toolkit's canonical Grover
    amplitude-amplification success obligation.) -/
theorem grover_finds_marked :
    Complex.normSq
        (amplitude (apply_unitary (denote circuit) (uniform_state 3)) ⟨7, by norm_num⟩)
      ≥ 1 - 1 / (2 ^ 3 : ℕ) :=
  grover_gate_optimal_success

end Exported.GateModel.Grover
