\* Governed, but jobs carry a REAL classical footprint the governor cannot see.
\* NeverExceedsCapacity still holds — admission is satisfied.
\* MachineNeverExhausted is EXPECTED TO FAIL: the box is exhausted anyway.
SPECIFICATION Spec
CONSTANTS
    Jobs <- MCJobs
    Capacity <- MCCapacity
    Weight <- MCWeight
    Classical <- MCClassicalReal
    Governed <- TRUE
INVARIANT
    TypeOK
    NeverExceedsCapacity
    MachineNeverExhausted
