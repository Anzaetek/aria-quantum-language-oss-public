[package]
name = "omega-app-guest"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[[bin]]
name = "omega_app"
path = "src/main.rs"

[dependencies]
serde = { version = "1", default-features = false, features = ["alloc", "derive"] }
serde_json = { version = "1", default-features = false, features = ["alloc"] }
